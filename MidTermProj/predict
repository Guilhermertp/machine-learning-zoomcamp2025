from flask import Flask, request, jsonify
import pickle
import pandas as pd

app = Flask(__name__)

# Load trained LightGBM model
with open("lgbm_model.pkl", "rb") as f:
    model = pickle.load(f)


@app.route("/predict", methods=["POST"])
def predict():
    try:
        data = request.json  # Input must be JSON

        # Convert to DataFrame
        df = pd.DataFrame([data])

        # Predict class and probability
        prediction = model.predict(df)[0]
        prob = model.predict_proba(df)[0][1]  # Probability of tsunami class

        return jsonify({
            "tsunami_prediction": int(prediction),
            "probability_of_tsunami": float(prob)
        })

    except Exception as e:
        return jsonify({"error": str(e)})


@app.route("/", methods=["GET"])
def home():
    return jsonify({"message": "Tsunami Prediction API Running"})


if __name__ == "__main__":
    app.run(host="0.0.0.0", port=5000)
